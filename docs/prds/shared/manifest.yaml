schema_version: 1
generated: 2026-04-21
last_updated: 2026-04-26
prd_source: "docs/prd/prd.md"

pieces:
  - name: spec-flow-v2.0.0
    description: Charter stage + codified docs structure + two NN namespaces (NN-C / NN-P) + CR coding rules. Seven internal pieces, shipped across commits 78991dc..cd1eb7a.
    prd_sections: [G-2, FR-004, FR-005, FR-006, FR-007, NFR-003, NN-P-001, NN-P-002]
    dependencies: []
    status: done
    notes: |
      Released 2026-04-20 as a single major version. Shipped commits:
      78991dc piece 1 - charter skill + templates + qa-charter agent
      598e015 piece 2 - templates + config + session-start doctrine load
      572d6d8 piece 3 - downstream skill charter wiring
      b177748 piece 4 - agent updates
      3db6de1 piece 5 - update mode + divergence resolution
      fe7355b piece 6 - retrofit mode + migration pipeline
      cd1eb7a piece 7 - README + consolidated CHANGELOG + version bump

  - name: PI-001-marketplace-version-sync
    description: Update `.claude-plugin/marketplace.json` to reflect spec-flow's actual version (2.0.0). Was stale at 1.1.1 — caught during charter bootstrap. First NN-C-001 violation in the history of the repo; fixing it was straightforward.
    prd_sections: [FR-001, NN-C-001]
    dependencies: []
    status: done
    notes: |
      Fixed 2026-04-21. marketplace.json spec-flow entry bumped 1.1.1 → 2.0.0
      to match plugins/spec-flow/.claude-plugin/plugin.json. No plugin-level
      version bump for this change since the edit is outside plugins/*/
      scope (NN-C-009 scope exclusion).

  - name: PI-002-version-sync-ci
    description: Minimal CI job (or pre-commit hook) that enforces NN-C-001 across all plugins in the marketplace. Compares `plugin.json` version to the matching `marketplace.json` entry. Fails if any plugin drifts. Shell + `jq` only.
    prd_sections: [FR-001, NFR-003, NN-C-001]
    dependencies: [PI-001-marketplace-version-sync]
    status: open

  - name: PI-003-charter-dogfood-lessons
    description: After using charter on a few real pieces of work (PI-001 and PI-002 themselves are good candidates), capture what worked and what didn't. Feed findings into spec-flow's improvement backlog — likely some are backlog items that land in a future spec-flow release.
    prd_sections: [G-4, NN-P-003, SC-003]
    dependencies: [PI-001-marketplace-version-sync, PI-002-version-sync-ci]
    status: open

  - name: PI-004-second-plugin-pilot
    description: Pilot a second, intentionally-small plugin in this marketplace to validate that the extension mechanism generalizes and that plugin isolation (the no-cross-plugin-references rule from charter/architecture.md) holds up in practice. Candidate plugins are intentionally undecided — the point is to pick something small enough to ship without a full spec-flow cycle.
    prd_sections: [G-1, G-3, SC-004]
    dependencies: []
    status: open

  - name: PI-005-copilot-cli-parity-map
    description: Produce docs/architecture/copilot-cli-parity.md — a reference document mapping every marketplace-level and plugin-internal Claude Code capability to its GitHub Copilot CLI equivalent, with conversion recipes and a DIRECT/APPROXIMATE/GAP tier classification per row. Every GAP row specifies a degradation strategy (INLINE-INTO-INSTRUCTIONS, DROP-WITH-WARNING, EMIT-SHIM, or HARD-ERROR). Desk research only; empirical dogfood deferred to a separate maintainer-controlled project during/after PI-006. Source-of-truth input for PI-006.
    prd_sections: [G-3]
    dependencies: []
    status: superseded
    notes: |
      Aborted during execute (2026-04-21) after the mapping document reached the 5-agent Final Review stage. Root cause: the piece's core premise — "convert Claude Code plugin artifacts to Copilot CLI equivalents on a mirror branch" — turned out to be the wrong architecture.

      Discovery: the superpowers plugin (github.com/obra/superpowers, v5.0.7) demonstrates an alternative "co-ship dual-configuration" pattern where a single source tree ships per-host manifests (`.claude-plugin/plugin.json`, `.cursor-plugin/plugin.json`, `gemini-extension.json`, `package.json`) side-by-side with per-host hook-registration files (`hooks/hooks.json`, `hooks/hooks-cursor.json`) and shared content directories (`skills/`, `agents/`, `commands/`). `AGENTS.md` is a symlink to `CLAUDE.md` — Copilot CLI reads the same instruction file Claude does with zero transformation. For distribution to a different canonical marketplace (e.g., OpenAI Codex), a single rsync-plus-exclude-plus-regenerate-overlay script handles publishing (see `scripts/sync-to-codex-plugin.sh` in superpowers). No mirror branch, no conversion pipeline.

      PI-005's feature branch `spec/PI-005-copilot-cli-parity-map` is preserved with full execute history (spec + plan + 217-line deliverable + all phase commits + fix-code commits from 3 QA iterations). Not merged. Reference for the next spec cycle.
      Successor: PI-007-copilot-coship (shipped 2026-04-21).

  - name: PI-006-copilot-mirror-ci
    description: CI/CD pipeline plus bash conversion script that encodes PI-005's mapping doc and produces a `master-copilot` branch mirroring `master` on every commit with Claude→Copilot conversions applied. Dogfooded in a separate maintainer-controlled project, not in this repo.
    prd_sections: [G-3]
    dependencies: [PI-005-copilot-cli-parity-map]
    status: superseded
    notes: |
      Aborted 2026-04-21 as part of the PI-005 abort. The mirror-branch conversion pipeline this piece proposed is the wrong pattern — the superpowers multi-host plugin demonstrates that co-shipping all hosts' manifests and hook-registrations in one tree eliminates the need for conversion entirely. See PI-005's notes for the discovery detail. Successor: PI-007-copilot-coship (shipped 2026-04-21).

  - name: PI-007-copilot-coship
    description: |
      Apply a superpowers-style co-ship pattern to the `spec-flow` plugin so GitHub Copilot CLI users can install and invoke it. Delivered as a single-path dual-host co-ship — one `plugins/spec-flow/` tree with plain-`.md` agent files serves both Claude Code and Copilot CLI. Copilot CLI users install via subdirectory syntax (`/plugin install jmontanari/ai-plugins:plugins/spec-flow`) or via the marketplace 2-step flow (`/plugin marketplace add jmontanari/ai-plugins` → `/plugin install spec-flow@shared-plugins`). No mirror branch, no sync scripts, no content translation. Supersedes PI-005 and PI-006 with a strictly simpler architecture. Three smoketests on Copilot CLI v1.0.34 recorded in `docs/specs/PI-007-copilot-coship/learnings.md` — install + `/spec-flow:status` (PASS), `/agents` discovery (PASS), `/agents` listing + frontmatter hygiene (PASS). Fixed a path-duplication bug in `metadata.pluginRoot` during review.
    prd_sections: [G-3]
    dependencies: []
    status: done

  - name: PI-008-multi-prd-v3.0.0
    description: |
      Multi-PRD support — restructures the spec-flow docs layout so a project can hold multiple PRDs in parallel under a single shared charter. Replaces today's single `docs/prd/` + flat `docs/specs/` with `docs/prds/<prd-slug>/{prd.md, manifest.yaml, specs/<piece-slug>/, backlog.md}` sibling namespaces. Adds:
        - PRD lifecycle states (drafting / active / shipped / archived) carried in the PRD's front-matter
        - Dual backlog: per-PRD `backlog.md` for capability-scoped items, global `docs/improvement-backlog.md` for cross-PRD learnings and spec-flow process findings
        - Short-slug naming for worktrees (`worktrees/prd-<prd-slug>/piece-<piece-slug>/`) and branches (`spec/<prd-slug>-<piece-slug>` etc.)
        - Automatic charter-drift detection in every skill's Phase-1 context load (no new user-facing skill; inline resolution via focused qa-spec mode)
        - Cross-PRD piece dependencies via qualified references (`depends_on: <prd-slug>/<piece-slug>`) — declarative only; orchestration stays manual for v3.0
        - Migration flow (new `/spec-flow:migrate` skill) that upgrades existing v1.x (`docs/prd.md` flat) and v2.x (`docs/prd/prd.md` + `docs/prd/manifest.yaml`) projects to the v3 layout using `git mv` to preserve history
      Breaking change — ships as spec-flow v3.0.0 (major bump per NN-C-003 and NN-C-009). Ad-hoc task work (lightweight spec→plan→execute for out-of-band work) is out of scope and deferred to a separate future PRD (captured in improvement-backlog.md).
    prd_sections: [G-2, FR-004, NFR-003, NN-P-001, NN-P-003, NN-C-003]
    dependencies: []
    status: merged
    notes: |
      Merged 2026-04-25. Delivered the spec-flow v3.0.0 multi-PRD layout: docs/prds/<prd-slug>/{prd.md, manifest.yaml, backlog.md, specs/<piece-slug>/} sibling namespaces under singular docs/charter/. New /spec-flow:migrate skill (370 LOC) handles v0/v1/v2 → v3 transitions via history-preserving git mv with --inspect (dry-run) and --force (override safety) flags. Three new reference docs (slug-validator, v3-path-conventions, charter-drift-check) factor shared rules. SessionStart hook emits a non-blocking yellow warning when layout_version is missing or pre-v3 (NN-C-005-compliant 6-scenario smoke test).
      NN-C-009 three-place version bump landed in commit 8ed685e (plugin.json + marketplace.json + CHANGELOG header all at 3.0.0). Final Review (5 parallel reviewers) returned: PRD-alignment + spec-compliance both 0 must-fix; architecture/edge-case/blind surfaced 11 distinct must-fix items, all applied in commit 40876e6 (hook layout_version regex robustness, slug minimum-length rule, charter-drift missing-field handling, 7 migrate-skill safety fixes including the critical missing `mkdir -p docs/prds`, execute structural-failure dependency refusal contract).
      Two intentional deferrals: (a) FR-005 branch-design ambiguity (spec lists 3 branches per piece vs v2 single-branch reality preserved in code) — captured in learnings.md as v3.1.0 spec-amendment candidate; (b) AC-15/AC-18 dog-food run + release commit — procedure documented in plugins/spec-flow/docs/release-v3.0.0.md, executed at human-driven release time per NN-P-003 framing.
      Reflection findings persisted to docs/specs/PI-008-multi-prd-v3.0.0/learnings.md (8 process-retro items, 6 future-opportunities). High-severity process-retro items (skip-predicate calibration, mid-piece Opus pass for ≥6-phase pieces, deferred-finding tracking) should migrate to global docs/improvement-backlog.md at release time.

  - name: pi-010-discovery
    description: |
      v3.2.0 — Synchronous discovery triage. Eliminates silent backlog deferrals by making the execution that finds new work also fix it. Today the pipeline writes deferred QA findings, "NOT COVERED — deferred" AC matrix rows, and reflection-future-opportunities findings to backlog files at end-of-piece, surfacing them only at the next piece's spec brainstorm. Operators have no signal that prerequisite work is undone until much later. This piece restructures discovery → resolution as a synchronous flow with operator triage at discovery time.

      Operator principle (binding): "the execution that found the work also fixes it." Inline plan amendment is the default response to discovery, not a separate skill. The piece bundles five distinct sub-areas as Phase Groups:

      Capability (user-visible):
        - **`/spec-flow:defer` skill** (sub-area F) — sole supported path for writing to backlog files. Records source piece, source phase, finding text, operator's rationale for non-blocking. After this lands, every other skill's automatic backlog-write code path is removed.

      Orchestrator (no new user-facing surface):
        - **depends_on enforcement at spec/plan time** (sub-area A) — `/spec-flow:spec` and `/spec-flow:plan` gain a precondition check that surfaces unmet `depends_on` to the operator with three triage options (pull-deps-in as Phase 0, fork to manifest piece, proceed with `--ignore-deps`). Recorded as a `## Dependency triage` section in the spec or plan file. Today only `execute` reads `depends_on`.
        - **AC matrix deferred-row rejection at Verify gate** (sub-area B) — verify-agent rejects any `NOT COVERED — deferred to ...` row by default. New required `Reason:` field with three values (`does-not-block-goal | requires-amendment | requires-fork`). Phase-in via `legacy_deferred_rows: true` plan flag for one release cycle, then strict.
        - **Discovery triage + inline plan amendment** (sub-area D, load-bearing) — new step in `skills/execute/SKILL.md` between phase-complete and next-phase-start. Aggregates discoveries from the just-finished phase's QA review and AC matrix gate; on each discovery, prompts operator with amend / fork / defer. The amend path dispatches a new `plan-amend` agent (Sonnet) that emits a unified diff inserting suffix-named phases (`phase_<n>_amend_<k>`) before the next original phase, then the orchestrator commits `amend(plan): <reason>` and resumes. Budget: 2 amendments per piece. qa-plan re-review on diff + neighborhood. Auto-mode default: amend if absorption < 50% of cumulative diff, escalate otherwise. New `.discovery-log.md` artifact per piece records every discovery and its triage outcome. Final-Review-board misses route through the same triage flow (re-opening the piece for amendment phases instead of writing deferred-finding stubs).
        - **Reflection agents emit triage reports** (sub-area E) — `reflection-future-opportunities` and `reflection-process-retro` stop writing directly to backlog files. They emit findings to the orchestrator at Step 4.5; orchestrator dispatches the same triage UX from sub-area D for each finding. Operator's `defer` choice invokes `/spec-flow:defer`. Process-retro `process-improvement` category items batch-confirm at end of piece (cross-PRD learnings get one batched defer prompt rather than per-finding triage).

      Architectural shape: 5 Phase Groups (A, B, C-as-D-prep, D, E+F bundled). Plan-amend agent is a new Sonnet agent under `plugins/spec-flow/agents/plan-amend.md`. Discovery-log artifact is a new per-piece file. All other changes modify existing skill / agent prose plus minor templates.

      Backwards-compatibility: minor bump (NN-C-003: additive + opt-in stricter behavior). The `legacy_deferred_rows: true` flag preserves today's AC matrix behavior for one release. Existing `backlog.md` entries are grandfathered (no one-time triage pass forced); operators may invoke a `--triage-existing-backlog` mode of /spec-flow:defer manually if desired (out of scope for this piece — captured for future).

      Risks called out for spec brainstorm:
        - Amendment loops bounded by per-piece budget (2). Hitting the budget escalates to "scope was wrong; abandon and re-spec."
        - Triage prompt fatigue mitigated by auto-mode defaults and the principle that better specs/plans (which this feature's adoption pressures) produce fewer discoveries.
        - Plan-amend agent's diff+neighborhood scope must include any phase that scopes the same SKILL.md block as an amended phase — inherits the cross-phase content-dependency lesson from pi-009 (Phase B.1 → B.4 collateral damage).
        - Spec amendments (vs plan amendments) handled identically but with stricter budget (1 per piece) and full qa-spec re-dispatch since spec drift has PRD impact.
    prd_sections: [FR-004, NFR-003, NFR-004, NN-P-002]
    dependencies: [pi-009-hardening]
    status: specced

  - name: pi-009-hardening
    description: |
      v3.1.0 — bundles all v3.0.0-reflection-driven follow-up work into one minor release. Single bigger piece by design: ships a coherent v3.1.0 rather than 3+ point releases that each pay the spec/plan/release ceremony cost. Slug renamed from PI-009-v3.1.0-hardening to comply with v3 slug-validator rules ([a-z0-9-]+, ≤20 chars). Spec authored 2026-04-25 in worktree spec/shared-pi-009-hardening; QA cleared after 3 iterations.

      Capability (user-visible):
        - FR-005 branch-design resolution — pick single-branch (current code reality) or three-branch (spec text). Update spec OR SKILL.md and add ACs covering the chosen model.
        - Charter-drift deep scan — extend `/spec-flow:status` with `--include-drift` mode that parses NN/CR citations in each spec and verifies them against current charter bodies (semantic drift, not just timestamp).
        - Worktree-token sweep — add `{{worktree_root}}` template token; replace hard-coded `worktrees/` references across all 17 implementer agent prompts.
        - Migrate-skill environment precondition — formally document "the migrate skill runs in an LLM environment with `yq`/`python3` available" as a precondition; update NFR-004 and clarify NN-C-002 scope. Path 2 (ship an embedded helper script) is deferred — it would need a charter brainstorm before being specced.

      Orchestrator (no user-visible surface change):
        - Sharpen Opus QA skip-predicate — skip only for additive markdown/YAML/config; default to Opus when phase touches shell/branching logic or new skill body.
        - Mid-piece Opus QA pass for ≥6-phase pieces — insert at half-way commit; reviewer reads cumulative diff vs. spec.
        - "Deferred to reflection" finding tracking — orchestrator appends stub to PRD-local backlog at deferral time so reflection agents see them as input.
        - Plan-skill phase-sizing rule — split phases >150 LOC of new behavioral prose into Phase Groups.
        - Plan-skill exit-gate semantics rule — "X ran" cannot downgrade to "X is documented to run later."
        - Default `[Verify]` commands to `python3 -c` for YAML/JSON validation (drop yq/jq dependency in pipeline self-checks).
        - Iter-until-clean QA loop, applied universally — every QA gate (spec, plan, charter, execute per-phase, mid-piece Opus, Final Review fix-up) keeps iterating until the reviewer returns no must-fix findings. Iter-1 is full review; iter-2+ is focused re-review on the diff. Today's `qa_iter2: auto` skip predicate is retired; the 3-iter circuit breaker stays as the escalation guard, not as auto-stop. Codified in a single shared reference doc that all QA-running skills cite.

      Minor bump (NN-C-003: backwards-compatible additions). 11 items, plan likely splits into 2-3 Phase Groups (capability vs. orchestrator vs. plan-skill rules) so per-area QA stays focused. Brainstorm during spec authoring will negotiate FR-005 path-pick and confirm migrate-skill stays on path 1.
    prd_sections: [FR-004, NFR-003, NFR-004]
    dependencies: [PI-008-multi-prd-v3.0.0]
    status: done

coverage:
  total_prd_sections: 15
  covered_sections: 8
  uncovered_sections:
    - G-1
    - G-3
    - FR-002
    - FR-003
    - NFR-001
    - NFR-002
    - NFR-004
    - SC-001
    - SC-002
    - SC-004
    - NN-P-003
  percentage: 53
  notes: |
    Uncovered-but-intentional: many of G-1/G-3/FR-002/FR-003/NFR-001/NFR-002/NFR-004 are already satisfied by the repo's existing structure (marketplace.json exists, plugin.json exists, install mechanism works, no runtime deps). These don't need dedicated pieces — they're verified by the existence and correctness of the current files. Charter-enforcement keeps them satisfied going forward.
    SC-001/SC-002/SC-003/SC-004 are measurement goals, not implementable pieces. They come true (or don't) over time as users adopt.
    NN-P-003 ("dog-food before recommend") is a process rule enforced at release time, not a piece.
    PI-005 (design) and PI-006 (implementation) were intended as the prerequisite pieces for SC-002, but both were superseded on 2026-04-21 when the mirror-branch conversion pattern they assumed was discovered to be inferior to the co-ship dual-configuration pattern demonstrated by the superpowers plugin (github.com/obra/superpowers). PI-007-copilot-coship shipped on 2026-04-21 as the design+implementation prerequisite for SC-002, using a single-path dual-host co-ship (plain-`.md` agent files, subdirectory and marketplace install paths, no mirror branch). SC-002 remains in uncovered_sections as a measurement goal — its Copilot-CLI install leg now has three documented smoketest PASS outcomes (see `docs/specs/PI-007-copilot-coship/learnings.md`), and the real-world-adoption signal will accumulate as users install spec-flow on Copilot CLI in the wild.
