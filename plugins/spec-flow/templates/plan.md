---
charter_snapshot:
  architecture: {{date}}
  non-negotiables: {{date}}
  tools: {{date}}
  processes: {{date}}
  flows: {{date}}
  coding-rules: {{date}}
legacy_deferred_rows: false  # OPT-IN: set to true to preserve pre-3.2.0 AC matrix behavior (silent acceptance of bare deferral rows). Deprecated — to be retired in v3.3.0.
fast: false                  # true = fast mode: skips per-phase QA agents (qa-tdd-red, qa-phase, qa-phase-lite),
                             # replaces per-phase verify agent with direct test-command shell run, adds
                             # verify Mode: Piece Full as 9th board member at Final Review.
                             # Use for: config/infra/scaffolding, moderate-complexity, non-security-critical work.
                             # Do NOT use for: auth, payments, compliance, or pieces > 12 phases.
---

<!-- {{piece_slug}} optional — defaults to kebab-cased {{piece_name}} -->
# Plan: {{piece_name}}

**Spec:** docs/prds/{{prd_slug}}/specs/{{piece_slug}}/spec.md
**Charter:** <charter_root>/skills/charter-*/SKILL.md (binding — each phase enumerates its honored NN-C/NN-P/CR entries)
**Status:** draft

## Overview
{{implementation_approach_summary}}

## Architectural Decisions

<!-- Generated by plan skill Phase 1 exploration; see plan SKILL.md ADR generation step -->
<!-- If no significant decisions: "No significant architectural decisions for this piece." -->

### ADR-1: {{decision_title}}
**Context:** {{why_this_decision_was_needed}}
**Decision:** {{what_was_decided}}
**Alternatives considered:** {{at_least_two_alternatives_with_rationale}}
**Consequences:** {{what_becomes_easier_harder_or_irreversible}}
**Charter alignment:** {{nn_c_or_cr_entries_this_decision_honors_or_constrains}}

## Phases

Each phase uses exactly ONE of two tracks:

- **TDD track** — phase contains `[TDD-Red]`. Use for behavior-bearing code that benefits from test-driven design.
- **Implement track** — phase contains `[Implement]` (and NO `[TDD-Red]`). Use for config, infrastructure, scaffolding, glue/wiring code, docs-as-code, fixtures, and migrations — anything where unit-level TDD would be ceremony without payoff. The `[Verify]` step on this track runs whatever command validates the work (lint, type check, build, smoke run, integration test) — the plan author picks.
- **Non-TDD mode** — the plan's front-matter declares `tdd: false`. ALL phases use `[Implement]` + `[Write-Tests]` structure. AC Coverage Matrix is not required. QA and Final Review remain fully intact.

A phase must have exactly one of these markers. The executor branches mechanically on the checkbox it finds.

## Integration-Test Registry (M1)

Built from plan + Red authoring (never from Build); carried across phases by execute; absent ⇒ no integrations declared (NFR-INT-02).

Columns `registered_in_phase` and `completes_in_phase` are plan-authored. Columns `skeleton_sha256` and `completed_sha256` are **runtime-populated** — left blank (`—`) at plan-authoring time; the orchestrator fills them into its own state (not into this table) when Red authors the skeleton and when the completing phase edit lands.

| ID | Path | Boundary (inside) | Doubled externals (contract test) | AC | registered_in_phase | completes_in_phase | skeleton_sha256 | completed_sha256 |
|----|------|-------------------|-----------------------------------|----|--------------------|---------------------|-----------------|------------------|
| {{int_id}} | {{test_path}} | {{inside_components}} | {{doubled_ext}}({{contract_test}}) | {{ac_id}} | {{registered_N}} | {{completing_N}} | — | — |

### Phase 1 (TDD track example): {{phase_name}}
**Exit Gate:** {{exit_criteria}}
**ACs Covered:** {{ac_list}}
**In scope:** {{explicit_scope_list}}
**NOT in scope:** {{explicit_exclusions_with_forward_phase_references}}
**Charter constraints honored in this phase:**
- {{nn_c_id_or_cr_id}} ({{short_name}}): {{how_this_phase_honors_it}}

- [ ] **[TDD-Red]** Write failing tests
  - {{test_details}}

- [ ] **[QA-Red]** Reject theater tests before Build
  - Review Red's authored tests against the theater-pattern catalog (tautology, mock-echo, truthy-only, no-assertion, name/body mismatch, implementation coupling, etc.)
  - Verify each test adversarially binds to its claimed AC ({{ac_list}})
  - On FAIL: one Red retry with findings surfaced, then escalate

- [ ] **[Build]** Write minimal code to pass
  - Order tasks in checkpoint progression (types → constructors → public API → internals → error paths). The implementer commits at each logical checkpoint; good ordering gives it natural boundaries.

  **Change Specifications:**

  **T-1: {{MODIFY|CREATE|DELETE}} {{file_path}}**
  - Anchor: {{semantic_anchor}} (lines {{range}})
  - Current:
    ```
    {{verbatim_current_code_with_line_numbers_5_to_15_lines}}
    ```
  - Target: {{concrete_change_description_with_signatures_and_types}}
  - Pattern:
    ```
    {{verbatim_code_block_from_pattern_catalog_3_to_10_lines}}
    ```
  - Done: {{per_change_done_criteria}}
  - Verify: {{per_change_verification_step}}

  **T-2: {{MODIFY|CREATE|DELETE}} {{file_path_2}}**
  ...

- [ ] **[Verify]** Confirm tests pass
  **Per-change checks:**
  - T-1: {{per_change_verification_command}} — Expected: {{expected}}
  - T-2: {{per_change_verification_command}} — Expected: {{expected}}
  **Phase-level check:**
  - Run: {{exact_test_command}}
  - Expected: {{specific_pass_count}} passed, 0 failed, 0 errors
  - Failure: any test failure output or unexpected warnings
  - Verify: no test files modified since TDD-Red step

- [ ] **[Integration-Test]** (completing-phase only) Complete + green the outer `[integration]` test
  - Boundary: {{which components are inside; which true externals are doubled}}
  - completes_in_phase: {{N}}
  - Contract tests: {{one per doubled true external}}
  - Run: {{real-wired-path test command}} — Expected: {{specific pass output}}
  - Note: omit this block on non-completing phases; the registered test stays skeleton-red until N.

- [ ] **[Refactor]** Clean up (scope: Phase 1 files only)
  - Check for: duplication, naming, extract helpers
  - Constraint: only modify files created/changed in this phase

- [ ] **[QA]** Phase review
  - Review against: {{ac_list}}
  - Diff baseline: git diff {{phase_start_tag}}..HEAD

### Phase 2 (Implement track example): {{phase_name}}
**Exit Gate:** {{exit_criteria}}
**ACs Covered:** {{ac_list}}
**In scope:** {{explicit_scope_list}}
**NOT in scope:** {{explicit_exclusions_with_forward_phase_references}}
**Charter constraints honored in this phase:**
- {{nn_c_id_or_cr_id}} ({{short_name}}): {{how_this_phase_honors_it}}

- [ ] **[Implement]** Write code per the plan
  - Order tasks in checkpoint progression (schema/types → core wiring → wrappers/adapters → edge paths). The implementer commits at each logical checkpoint; good ordering gives it natural boundaries.
  - Architecture constraints this phase must honor: {{arch_constraints}}

  **Change Specifications:**

  **T-1: {{MODIFY|CREATE|DELETE}} {{file_path}}**
  - Anchor: {{semantic_anchor}} (lines {{range}})
  - Current:
    ```
    {{verbatim_current_code_with_line_numbers_5_to_15_lines}}
    ```
  - Target: {{concrete_change_description_with_signatures_and_types}}
  - Pattern:
    ```
    {{verbatim_code_block_from_pattern_catalog_3_to_10_lines}}
    ```
  - Done: {{per_change_done_criteria}}
  - Verify: {{per_change_verification_step}}

  **T-2: {{MODIFY|CREATE|DELETE}} {{file_path_2}}**
  ...

- [ ] **[Verify]** Confirm the implementation is sound
  **Per-change checks:**
  - T-1: {{per_change_verification_command}} — Expected: {{expected}}
  - T-2: {{per_change_verification_command}} — Expected: {{expected}}
  **Phase-level check:**
  - Run: {{exact_verification_command}}  (e.g. `ruff check .`, `tsc --noEmit`, `terraform validate`, `make build`, `pytest tests/integration/...`) — For YAML/JSON validation: use LLM-agent-step framing (e.g., "Read the file at <path> and confirm it parses as valid YAML/JSON; report any error inline") rather than yq/jq/language-specific runtime shell-outs. For other validations (lint, type check, build, smoke run): standard shell commands are fine.
  - Expected: {{specific_expected_output_with_values}}
  - Failure: {{what_output_means_failure}}

- [ ] **[Integration-Test]** (completing-phase only) Complete + green the outer `[integration]` test
  - Boundary: {{which components are inside; which true externals are doubled}}
  - completes_in_phase: {{N}}
  - Contract tests: {{one per doubled true external}}
  - Run: {{real-wired-path test command}} — Expected: {{specific pass output}}
  - Note: omit this block on non-completing phases; the registered test stays skeleton-red until N.

- [ ] **[Refactor]** (optional — include only if cleanup is likely needed) Clean up (scope: Phase 2 files only)
  - Check for: duplication, naming, extract helpers
  - Constraint: only modify files created/changed in this phase

- [ ] **[QA]** Phase review
  - Review against: {{ac_list}}
  - Diff baseline: git diff {{phase_start_tag}}..HEAD

### Phase 2 (Non-TDD mode): {{phase_name}}
**Exit Gate:** {{exit_criteria}}
**ACs Covered:** {{ac_list}}
**In scope:** {{explicit_scope_list}}
**NOT in scope:** {{explicit_exclusions_with_forward_phase_references}}
**Charter constraints honored in this phase:**
- {{nn_c_id_or_cr_id}} ({{short_name}}): {{how_this_phase_honors_it}}

- [ ] **[Implement]** Write code per the plan
    - Order tasks in checkpoint progression (schema/types -> core wiring -> wrappers/adapters -> edge paths). The implementer commits at each logical checkpoint; good ordering gives it natural boundaries.
    - Architecture constraints this phase must honor: {{arch_constraints}}

    **Change Specifications:**

    **T-1: {{MODIFY|CREATE|DELETE}} {{file_path}}**
    - Anchor: {{semantic_anchor}} (lines {{range}})
    - Current:
      ```
      {{verbatim_current_code_with_line_numbers_5_to_15_lines}}
      ```
    - Target: {{concrete_change_description_with_signatures_and_types}}
    - Pattern:
      ```
      {{verbatim_code_block_from_pattern_catalog_3_to_10_lines}}
      ```
    - Done: {{per_change_done_criteria}}
    - Verify: {{per_change_verification_step}}

    **T-2: {{MODIFY|CREATE|DELETE}} {{file_path_2}}**
    ...

- [ ] **[Write-Tests]** Write tests for the implemented code
    - No "fail first" requirement — tests are written for existing code.
    - Aim for reasonable coverage of the phase's ACs.
    - Stage tests via `git add` (do NOT commit) so Verify can run them.

- [ ] **[Verify]** Confirm the implementation is sound
    **Per-change checks:**
    - T-1: {{per_change_verification_command}} — Expected: {{expected}}
    - T-2: {{per_change_verification_command}} — Expected: {{expected}}
    **Phase-level check:**
    - Run: {{exact_verification_command}}     (e.g. `ruff check .`, `tsc --noEmit`, `terraform validate`, `make build`, `pytest tests/integration/...`)
    - Expected: {{specific_expected_output_with_values}}
    - Failure: {{what_output_means_failure}}

- [ ] **[Integration-Test]** (completing-phase only) Complete + green the outer `[integration]` test
    - Boundary: {{which components are inside; which true externals are doubled}}
    - completes_in_phase: {{N}}
    - Contract tests: {{one per doubled true external}}
    - Run: {{real-wired-path test command}} — Expected: {{specific pass output}}
    - Note: omit this block on non-completing phases; the registered test stays skeleton-red until N.

- [ ] **[Refactor]** (optional — include only if cleanup is likely needed) Clean up (scope: Phase N files only)
    - Check for: duplication, naming, extract helpers
    - Constraint: only modify files created/changed in this phase

- [ ] **[QA]** Phase review
    - Review against: {{ac_list}}
    - Diff baseline: git diff {{phase_start_tag}}..HEAD

A non-TDD phase uses `[Implement]` instead of `[TDD-Red]`, skips `[QA-Red]` and `[Build]`, and adds `[Write-Tests]` after implementation. No AC Coverage Matrix is required. Non-TDD mode is declared in the plan's front-matter as `tdd: false`.

## Phase Group A (example): {{group_name}}
**Exit Gate:** all sub-phases pass their oracles + group Deep QA clean
**ACs Covered:** {{group_ac_list}}

#### Sub-Phase A.1 [P]: {{sub_phase_name}}
**Scope:** {{literal_file_paths_comma_separated}}
**ACs:** {{sub_phase_ac_subset}}

- [ ] **[TDD-Red]** Write failing tests for this sub-phase
  - {{test_details}}

- [ ] **[QA-Red]** Reject theater tests before Build
  - Review Red's authored tests against the theater-pattern catalog
  - Verify adversarial binding to this sub-phase's ACs ({{sub_phase_ac_subset}})
  - On FAIL: one Red retry with findings surfaced, then escalate

- [ ] **[Build]** Implement this sub-phase
  - Order bullets in checkpoint progression (types → constructors → public API → internals → error paths). The implementer commits at each logical checkpoint; good ordering gives it natural boundaries.
  - {{implementation_details}}

- [ ] **[Verify]** Confirm tests pass
  - Run: {{test_command}}
  - Expected: all tests for this sub-phase pass
  - Verify: no test files modified since [TDD-Red] step

- [ ] **[QA-lite]** Sonnet narrow review
  - Scope: this sub-phase only
  - Review: plan alignment, AC matrix spot-check, structural sanity, scope discipline

#### Sub-Phase A.2 [P]: {{sub_phase_name}}
**Scope:** {{literal_file_paths_comma_separated}}
... (same shape as A.1)

#### Group-level tasks
- [ ] **[Refactor]** (optional — auto-skipped when all Builds clean)
  - Scope: union of all sub-phase files in this group
  - Check for: cross-sub-phase dedup opportunities, inconsistent naming
  - Constraint: only modify files created/changed in this group

- [ ] **[QA]** Opus deep review
  - Review against: group ACs (union)
  - Diff baseline: git diff {{group_start_tag}}..HEAD
  - Surface map composed by orchestrator (Files changed, Public symbols, Integration callers)

- [ ] **[Progress]** Single commit for the group

## AC Coverage Matrix

<!-- Generated by plan skill after all phases are drafted; see plan SKILL.md AC matrix step -->
<!-- One row per spec AC. Status: COVERED or NOT COVERED. NOT COVERED rows require a forward pointer. -->

| AC ID | Summary | Status | Covered By |
|-------|---------|--------|------------|
| {{ac_id}} | {{one_line_summary}} | COVERED | {{phase_reference}} |
| {{ac_id}} | {{one_line_summary}} | NOT COVERED | — Forward pointer: `{{future_piece_slug_or_justification}}` |

## Executable AC Binding

<!-- Generated by plan skill after AC Coverage Matrix; maps each COVERED AC to its exact verification. -->
<!-- This table is the contract between plan and execute — if a phase cannot fill in Command/Check with something concrete, the [Verify] block needs more specificity. -->

| AC ID | Verification Type | Command/Check | Expected Result |
|-------|------------------|---------------|-----------------|
| {{ac_id}} | shell | `{{exact_shell_command}}` | {{specific_expected_output}} |
| {{ac_id}} | file-check | `test -f {{file_path}}` | File exists at path |
| {{ac_id}} | agent-step | Read `{{file_path}}` and confirm {{specific_criterion}} | {{criterion_passes}} |

## Contracts

<!-- Generated by plan skill after all phases are drafted; see plan SKILL.md Contracts step -->
<!-- One entry per boundary-crossing interface in TDD-track phases. -->
<!-- TDD phases with no boundary-crossing interfaces: document omission with rationale. -->
<!-- Implement-only plans: include section with "No TDD-track phases" note. -->

### C-1: {{interface_name}}
- **ID:** C-1
- **Type:** Function | API Endpoint | Event Schema | Data Schema
- **Phase:** {{phase_reference}}
- **Signature:** `{{function_or_endpoint_signature_with_types}}`
- **Inputs:** `{{param}}: {{type}}` — {{description}}
- **Outputs:** `{{return_type}}` — {{description}}
- **Error cases:** `{{error_condition}}` → {{behavior_or_exception}}
- **Constraints:** {{invariants_preconditions_postconditions}}

## Parallel Execution Notes
{{parallel_notes}}

## Agent Context Summary
| Task Type | Receives | Does NOT receive |
|-----------|----------|-----------------|
| TDD-Red | Phase requirements, test patterns, spec ACs | Implementation code, prior conversation. **Stages** tests (does NOT commit — v2.7.0+); the implementer's unified commit captures Red's staged tests + Build's production code together. |
| QA-TDD-Red | Red's `## Tests Written` list, authored test source (read from staging area / working tree), phase's [TDD-Red] block, phase ACs, Red's oracle block | Production source, prior phases' tests, brainstorming history |
| Implementer (Mode: TDD) | `Mode: TDD` flag, failing tests (verbatim), Red's `## Staged test manifest` (paths + SHA-256), plan details, arch constraints, pattern pointers, codebase context from `introspection.md` (Dependency Map + Test Landscape for phase scope). Working tree starts with Red's tests already staged. | Spec rationale, brainstorming history. **Creates ONE unified commit** containing Red's staged tests + Build's production code; orchestrator verifies integrity via SHA-256 re-hash and file-list reconciliation post-commit. |
| Implementer (Mode: Implement) | `Mode: Implement` flag, plan [Implement] tasks, spec ACs, plan's [Verify] command, arch constraints, pattern pointers, codebase context from `introspection.md` (Dependency Map + Test Landscape for phase scope) | Spec rationale, brainstorming history |
| Verify | Verification output (tests or plan-specified command), spec ACs | Implementation reasoning |
| QA-lite (sub-phase) | `Mode:` flag, sub-phase diff, sub-phase ACs, AC matrix (from Build), sub-phase scope block | Full piece spec, PRD sections, other sub-phases' diffs |
| Refactor | Current code (phase files only), mode's verification command, quality principles | Prior agent conversations |
| QA | Phase diff, spec, plan, PRD sections | Any agent conversation history |
